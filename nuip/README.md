# `greedBoth.cpp`

```
g++ -std=gnu++14 -O3 greedBoth.cpp
./a.out ../problemsL/FA022_tgt.mdl 
```

をするとカレントディレクトリに `FA022.nbt` と `FD022.nbt` ができます。

# `dis.cpp`

```
g++ -std=gnu++14 -O3 -DEXPORTBOTH dis.cpp
./a.out ../problemsL/FA022_tgt.mdl 
```

をするとカレントディレクトリに `FA022.nbt` と `FD022.nbt` ができます。